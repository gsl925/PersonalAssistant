# SDD：跨專案進度同步文件協定（PROGRESS.md）

> 交付對象：每個被 Personal Assistant（使用者的個人助理系統）追蹤的專案。請把這份文件放進你的專案，照著實作。

## 1. 目的

你的專案在自己的 repo 裡維護一份 `PROGRESS.md`，作為跟 Personal Assistant（以下簡稱 PA）溝通進度與決策的唯一介面。你（專案端的 Agent）不需要、也不應該直接呼叫任何外部系統或 API——所有溝通都透過讀寫這一份文件完成。

PA 那一端（多久巡一次、怎麼轉發 Telegram 等）不在本規格範圍內，你只需要照這份規格讀寫檔案。PA 是「郵差」，只負責轉發跟寫檔，**不會**跑進你的專案執行任何程式碼或指令——所有實際判斷跟動手都是你自己的事。

## 2. 檔案位置與格式

- 路徑：專案 repo 根目錄下的 `PROGRESS.md`（檔名固定，UTF-8，Markdown）。
- 若檔案不存在：你第一次執行時應自行建立，套用第 3 節的模板。
- 檔案只有三個一級區塊（`##` 標題文字必須逐字一致，PA 端靠這個字串比對），不要新增/刪除/重新命名這三個區塊。
- 如果你的專案原本已經有一份用途不同的 `PROGRESS.md`（例如自己的開發日誌），**請先把那份改名（例如 `DEV_LOG.md`），釋出 `PROGRESS.md` 這個檔名給這個協定專用**，不要把兩種用途混在同一個檔案裡。
- **把 `PROGRESS.md` 加進你專案的 `.gitignore`。** 這份檔案會被 PA 跟你雙方持續讀寫（checkbox 狀態、每次覆寫的進度回報），本質是常駐同步狀態，不是要保留歷史的文件，不應該進版控——commit 這種每 30 分鐘就可能變動的檔案只會製造雜訊。

## 3. 檔案結構模板

```markdown
# {專案名稱} 進度同步文件

## 📋 進度回報
- 更新時間: YYYY-MM-DD HH:MM
- 今天完成:
  - ...
- 明天預計:
  - ...

## 💬 待溝通 / 建議
- [ ] YYYY-MM-DD 問題或建議的一句話描述

## 📮 你的指示
- [ ] YYYY-MM-DD 使用者指示的一句話描述
```

## 4. 三個區塊的讀寫權限（嚴格分工，不可越界）

| 區塊 | 誰可以寫文字 | 誰只能打勾 |
|---|---|---|
| 📋 進度回報 | 你（專案 Agent） | PA（只讀，不寫） |
| 💬 待溝通 / 建議 | 你（新增新項目） | PA（轉發後打勾，代表「已轉達給使用者」，不代表「使用者已經決策」） |
| 📮 你的指示 | PA（把使用者的回覆寫進來） | 你（處理完打勾，代表「已處理」） |

**你絕對不要**修改「📮 你的指示」裡的文字內容或刪除項目，只能把處理完的項目狀態改成 `- [x]`。同理也不要動「💬 待溝通/建議」裡已經是 `[x]` 的舊項目文字（當作歷史記錄保留）。

## 5. 你（專案 Agent）該做的事

### 5.1 檢查「📮 你的指示」
建議在每次你的 Claude Code session 開始時，或你自己既有的排程機制觸發時，檢查一次：
1. 找出所有 `- [ ]`（未勾選）的項目，視為使用者尚未處理完的指示。
2. 自行判斷如何處理（調整優先度、回答問題、或實際去做）。
3. 處理完後改成 `- [x]`（不要刪除、不要改動文字）。若暫時無法完成，可以保留 `[ ]` 不動，下次繼續判斷。

### 5.2 更新「📋 進度回報」
建議在工作階段結束前，覆寫「更新時間」、「今天完成」、「明天預計」三個欄位——這個區塊每次都是**覆寫**，不是累積歷史。

### 5.3 寫「💬 待溝通 / 建議」
有問題需要使用者決策時（不限於工作階段開始/結束，隨時都可以）：
1. 在區塊最下方新增一行 `- [ ] YYYY-MM-DD 描述`。
2. 描述用一句話講清楚要使用者決策什麼——PA 會把這句話原樣轉發給使用者，不要寫成一大段。
3. 不要自己把這個項目打勾——打勾只由 PA 負責。

## 6. 邊界情況

- **檔案格式跑掉**（例如區塊標題被改掉）：PA 讀不到對應區塊時會視為「今天沒有更新」，不會報錯中斷，但這個專案的溝通會停擺，請維持第 2、3 節的標題字串不要變動。
- **「📋 進度回報」今天沒更新**（更新時間是舊的）：PA 不會重複轉發同一份舊內容，也不會自己編造內容。
- **PA 大約每 30 分鐘巡一次所有專案**，所以「💬」的新項目最慢 30 分鐘內會被轉發＋打勾。如果使用者一直沒有透過 Telegram 回覆（也就是「📮 你的指示」一直沒有出現對應的新項目），PA 會在自己的 Dashboard 另外建一筆代辦提醒使用者——這不需要你做任何事，純粹讓使用者不會漏看。
- 「💬 待溝通/建議」跟「📮 你的指示」建議**不要無限累積**——舊的已處理項目（`[x]`）可以視情況清理，避免檔案無限長；清理是你自己的維護工作，不影響 PA 端的判讀邏輯。

## 7. 完整範例

```markdown
# GenAI News 進度同步文件

## 📋 進度回報
- 更新時間: 2026-07-29 18:00
- 今天完成:
  - 修好 RSS 來源解析錯誤（feed encoding 問題）
  - 新增兩個追蹤來源
- 明天預計:
  - 開始做去重邏輯的單元測試

## 💬 待溝通 / 建議
- [ ] 2026-07-29 目前每天抓到重複新聞比例偏高，要不要把去重門檻從 0.85 調到 0.9？會影響召回率，需要你決定要不要接受漏掉一些邊緣案例。

## 📮 你的指示
- [x] 2026-07-25 先不用管效能優化，先把功能做完（已處理：功能優先）
- [ ] 2026-07-29 請優先處理上面提到的去重門檻問題，傾向調高沒關係
```
