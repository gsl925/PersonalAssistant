# SDD：跨專案進度同步文件協定（PROGRESS.md）

> 交付對象：每個被 Personal Assistant（使用者的個人助理系統）追蹤的專案。請把這份文件放進你的專案，照著實作。

## 1. 目的

你的專案在自己的 repo 裡維護一份 `PROGRESS.md`，作為跟 Personal Assistant（以下簡稱 PA）溝通進度與決策的唯一介面。你（專案端的 Agent）不需要、也不應該直接呼叫任何外部系統或 API——所有溝通都透過讀寫這一份文件完成。

PA 那一端（多久巡一次、怎麼轉發 Telegram 等）不在本規格範圍內，你只需要照這份規格讀寫檔案。PA 是「郵差」，只負責轉發跟寫檔，**不會**跑進你的專案執行任何程式碼或指令——所有實際判斷跟動手都是你自己的事。

## 2. 檔案位置與格式

- 路徑：專案 repo 根目錄下的 `PROGRESS.md`（檔名固定，UTF-8，Markdown）。
- 若檔案不存在：你第一次執行時應自行建立，套用第 3 節的模板。
- 檔案只有三個一級區塊（`##` 標題文字必須逐字一致，PA 端靠這個字串比對），不要新增/刪除/重新命名這三個區塊。
- 如果你的專案原本已經有一份用途不同的 `PROGRESS.md`（例如自己的開發日誌），**請先把那份改名（例如 `DEV_LOG.md`），釋出 `PROGRESS.md` 這個檔名給這個協定專用**，不要把兩種用途混在同一個檔案裡。
- **把 `PROGRESS.md` 加進你專案的 `.gitignore`。** 這份檔案會被 PA 跟你雙方持續讀寫（checkbox 狀態、每次覆寫的進度回報），本質是常駐同步狀態，不是要保留歷史的文件，不應該進版控——commit 這種每 30 分鐘就可能變動的檔案只會製造雜訊。
- 另外還有一份 `PROGRESS_HISTORY.md`，**這份要正常進版控，不要 gitignore**——完全由 PA 寫入（每次「💬 待溝通/建議」真正被你回覆解決時，PA 會把問題跟你的回覆一起追加寫進這份檔案，當作永久決策紀錄），你只需要讀取當作歷史脈絡參考，不需要自己維護或寫入它。

## 3. 檔案結構模板

```markdown
# {專案名稱} 進度同步文件

## 📋 進度回報
- 更新時間: YYYY-MM-DD HH:MM
- 今天完成:
  - ...
- 明天預計:
  - ...

## 💬 待溝通 / 建議
- [ ] YYYY-MM-DD 問題或建議的一句話描述

## 📮 你的指示
- [ ] YYYY-MM-DD 使用者指示的一句話描述
```

## 4. 三個區塊的讀寫權限（嚴格分工，不可越界）

| 區塊 | 誰可以寫文字 | 誰只能打勾 |
|---|---|---|
| 📋 進度回報 | 你（專案 Agent） | PA（只讀，不寫） |
| 💬 待溝通 / 建議 | 你（新增新項目） | PA（**只有使用者真的回覆解決後才打勾**，代表「已解決」——單純轉發不會打勾） |
| 📮 你的指示 | PA（把使用者的回覆寫進來） | 你（處理完打勾，代表「已處理」） |

**你絕對不要**修改「📮 你的指示」裡的文字內容或刪除項目，只能把處理完的項目狀態改成 `- [x]`。同理也不要動「💬 待溝通/建議」裡已經是 `[x]` 的舊項目文字（當作歷史記錄保留）。

## 5. 你（專案 Agent）該做的事

### 5.1 檢查「📮 你的指示」
建議在每次你的 Claude Code session 開始時，或你自己既有的排程機制觸發時，檢查一次：
1. 找出所有 `- [ ]`（未勾選）的項目，視為使用者尚未處理完的指示。
2. 自行判斷如何處理（調整優先度、回答問題、或實際去做）。
3. 處理完後改成 `- [x]`（不要刪除、不要改動文字）。若暫時無法完成，可以保留 `[ ]` 不動，下次繼續判斷。

### 5.2 更新「📋 進度回報」
建議在工作階段結束前，覆寫「更新時間」、「今天完成」、「明天預計」三個欄位——這個區塊每次都是**覆寫**，不是累積歷史。

### 5.3 寫「💬 待溝通 / 建議」
有問題需要使用者決策時（不限於工作階段開始/結束，隨時都可以）：
1. 在區塊最下方新增一行 `- [ ] YYYY-MM-DD 描述`。
2. 描述用一句話講清楚要使用者決策什麼——PA 會把這句話原樣轉發給使用者，不要寫成一大段。
3. 不要自己把這個項目打勾——打勾只由 PA 負責，而且只有使用者真的回覆之後才會打勾（見第 6 節）。
4. 同一時間可以有多筆待決策項目並存；PA 轉發時會各自帶一個編號（例如「#2 需要你決定：...」），這是 PA 內部用來精準對應「使用者回覆的是哪一筆」的機制，你不需要自己編號，也不用管這個編號。

## 6. 邊界情況

- **檔案格式跑掉**（例如區塊標題被改掉）：PA 讀不到對應區塊時會視為「今天沒有更新」，不會報錯中斷，但這個專案的溝通會停擺，請維持第 2、3 節的標題字串不要變動。
- **「📋 進度回報」今天沒更新**（更新時間是舊的）：PA 不會重複轉發同一份舊內容，也不會自己編造內容。
- **PA 大約每 30 分鐘巡一次所有專案**，所以「💬」的新項目最慢 30 分鐘內會被轉發（**轉發不會打勾**，只有使用者真的回覆才會）。如果使用者遲遲沒有透過 Telegram 回覆，PA 會在自己的 Dashboard 另外建一筆代辦提醒使用者，但這筆項目在 `PROGRESS.md` 裡仍然保持未勾選——這不需要你做任何事，純粹讓使用者不會漏看；使用者之後如果回覆了，還是會正常解決並打勾。
- 「💬 待溝通/建議」跟「📮 你的指示」建議**不要無限累積**——舊的已處理項目（`[x]`）可以視情況清理，避免檔案無限長；清理是你自己的維護工作，不影響 PA 端的判讀邏輯。

## 7. 完整範例

```markdown
# GenAI News 進度同步文件

## 📋 進度回報
- 更新時間: 2026-07-29 18:00
- 今天完成:
  - 修好 RSS 來源解析錯誤（feed encoding 問題）
  - 新增兩個追蹤來源
- 明天預計:
  - 開始做去重邏輯的單元測試

## 💬 待溝通 / 建議
- [ ] 2026-07-29 目前每天抓到重複新聞比例偏高，要不要把去重門檻從 0.85 調到 0.9？會影響召回率，需要你決定要不要接受漏掉一些邊緣案例。

## 📮 你的指示
- [x] 2026-07-25 先不用管效能優化，先把功能做完（已處理：功能優先）
- [ ] 2026-07-29 請優先處理上面提到的去重門檻問題，傾向調高沒關係
```

## 8. `PROGRESS_HISTORY.md`（決策歷史紀錄，PA 寫入，正常進版控）

跟 `PROGRESS.md` 不同，這份檔案**只會累積、不會被覆寫**，每次「💬」項目被使用者真正回覆解決時，PA 會自動追加一筆紀錄：

```markdown
# {專案名稱} 進度同步歷史紀錄

## 2026-07-29 — GenAI News
- **待溝通**：目前每天抓到重複新聞比例偏高，要不要把去重門檻從 0.85 調到 0.9？
- **決策**：好，調到 0.9，接受漏掉一些邊緣案例
```

你（專案 Agent）**不需要寫這份檔案**，只需要在需要回顧過去決策脈絡時讀取它即可；它也不用加進 `.gitignore`，正常 commit 進 repo，當作這個專案跟使用者之間的永久決策記錄。

## 9. 專案端主動排程（互動式 Claude Code session 自我喚醒，選配）

第 5 節講的是「session 開始時檢查一次」。如果某個專案想要更即時的監看，可以在自己的 `CLAUDE.md` 裡加一段指示，讓互動式 session 在完成一次性檢查後，用 `/loop`（或等效的排程 wake-up 工具）建立一個固定間隔（例如每 30 分鐘）的自我喚醒排程，重複執行第 5 節的檢查／處理／回報流程，直到這個 session 結束或使用者停止排程。

這跟 PA 自己的巡邏（第 6 節，約 30 分鐘一次讀檔＋轉發）以及 PA 選配的 `auto_wake`（對啟用該功能的專案，用 headless `claude -p` 呼叫喚醒你，見 `claude_wake.py`）是三個獨立、互不知情的機制，可能同時對同一份 `PROGRESS.md` 動作。這是刻意允許的——每一層都是各自獨立的安全網，任何一層沒開或掛掉都不影響其他層——但必須遵守：

1. **永遠先重新讀檔、再決定要不要動作**——不要沿用記憶體裡舊版本的內容去覆寫檔案；每次要處理或寫入前重新讀取最新內容，降低覆蓋別的 actor 剛寫入的東西的機率。
2. **處理「📮 你的指示」項目前，先確認重新讀檔後它還是 `- [ ]`**——如果已經被別的 actor 改成 `- [x]`，代表已經有人處理過了，直接跳過，**不要重複執行一次有副作用的動作**（例如重複發送通知、重複寄信）。
3. 「📋 進度回報」是覆寫語意，多個 actor 交錯寫入時，只會留下最後一次寫入的內容——這是已知、可接受的限制，不需要特別處理或報錯。
4. 互動式自我喚醒**只在那個 session 存活期間有效**（關掉終端機/視窗，排程就停了），不是常駐機制；如果需要不依賴任何終端機存活的保障，還是要靠 PA 的巡邏或 `auto_wake`。
