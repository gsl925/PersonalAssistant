# Personal Assistant — CLAUDE.md

## 跨專案進度同步協定（PROGRESS.md）

這個 repo 本身也是一個受 PA（這個專案自己）追蹤進度的專案——PA 一邊是「郵差」（讀寫其他專案的 PROGRESS.md），一邊自己也開了一份 `PROGRESS.md` 給使用者留言用。協定規格見本repo `SDD_PROGRESS_SYNC.md`；本節是把該規格的行為義務落實到這個專案的操作守則。

`PROGRESS.md` 位於本專案根目錄，是跟 PA 使用者溝通進度與決策的唯一介面，已加入 `.gitignore`（不進版控，因為是常駐同步狀態，每次覆寫）。檔案只有三個一級區塊，標題文字必須逐字保留：`## 📋 進度回報`、`## 💬 待溝通 / 建議`、`## 📮 你的指示`。三個區塊的讀寫權限嚴格分工：

| 區塊 | 你（Agent）可以做 | 你不能做 |
|---|---|---|
| 📋 進度回報 | 整段覆寫（不是累加） | — |
| 💬 待溝通 / 建議 | 在最下方新增一行 `- [ ] YYYY-MM-DD 一句話描述` | 不要自己打勾——打勾只由 PA 負責，而且**只有使用者真的回覆解決後才會打勾**，單純轉發不會打勾 |
| 📮 你的指示 | 把處理完的項目改成 `- [x]` | 不要修改文字內容、不要刪除項目 |

另外還有一份 `PROGRESS_HISTORY.md`，**正常進版控，不要 gitignore**——完全由 PA 寫入（每次「💬」項目真正被使用者回覆解決時，PA 會把問題跟決策追加寫進這份檔案，當作永久決策紀錄）。你不需要寫它，只在需要回顧過去決策脈絡時讀取即可。

### Session 開始時

檢查 `PROGRESS.md` 的「📮 你的指示」區塊，找出所有 `- [ ]`（未勾選）的項目——這些是使用者尚未處理完的指示：
1. 自行判斷如何處理（調整優先度、回答問題、或實際去做）。
2. 處理完後把該行改成 `- [x]`（絕對不要改動文字、不要刪除）。
3. 若這個 session 暫時無法完成，保留 `- [ ]` 不動即可，下次 session 再繼續判斷。

### Session 結束前

覆寫「📋 進度回報」的三個欄位（更新時間、今天完成、明天預計）。這是整段覆寫，不是累積歷史——不要保留舊的「今天完成」內容。

### 需要使用者決策時（隨時，不限 session 開始/結束）

在「💬 待溝通 / 建議」區塊最下方新增一行 `- [ ] YYYY-MM-DD 一句話描述`，把要使用者決策的事講清楚——PA 會原樣轉發給使用者，不要寫成一大段。不要自己把這個項目打勾；同一時間可以有多筆待決策項目並存，PA 轉發時會各自帶編號以對應使用者回覆的是哪一筆，這是 PA 內部機制，你不需要自己編號、也不用管。

### 邊界情況

- 三個區塊的 `##` 標題字串不能改動，否則 PA 讀不到對應區塊會視為「今天沒有更新」。
- 💬 的項目最慢 30 分鐘內會被 PA 轉發，但**轉發不會打勾**——只有使用者真的回覆之後才會打勾；使用者遲遲沒回覆時 PA 會自己在 Dashboard 建代辦提醒，這不需要你做任何事。
- `💬 待溝通/建議` 跟 `📮 你的指示` 裡已經是 `[x]` 的舊項目，文字當作歷史記錄保留；可視情況清理避免檔案無限長，但這是維護性操作，不影響 PA 端判讀邏輯。
